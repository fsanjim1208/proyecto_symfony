<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">
            <link href="{{ asset('CSS/Plantilla/css/bootstrap.min.css') }}" rel="stylesheet">
            <link href="{{ asset('CSS/Plantilla/css/style.css') }}" type="text/css" rel="stylesheet" />
            <link href="{{ asset('CSS/Plantilla/Animate/animate.min.css') }}" rel="stylesheet">
            <link href="{{ asset('CSS/Plantilla/assets/owl.carousel.min.css') }}" rel="stylesheet">
            {# <link href="img/favicon.ico" rel="icon"> #}
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600&family=Nunito:wght@600;700;800&display=swap" rel="stylesheet">
            <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
            <link href="{{ asset('CSS/cssLogin.css') }}" type="text/css" rel="stylesheet" />
            <link href="{{ asset('CSS/footer.css') }}" type="text/css" rel="stylesheet" />
            <link href="{{ asset('CSS/css.css') }}" type="text/css" rel="stylesheet" />
            <link href="{{ asset('CSS/jquery-ui.css') }}" type="text/css" rel="stylesheet" />
            <link href="{{ asset('CSS/jquery-ui.structure.css') }}" type="text/css" rel="stylesheet" />
            <link href="{{ asset('CSS/jquery-ui.theme.css') }}" type="text/css" rel="stylesheet" />

            
        {% endblock %}


            {# <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js" type="text/javascript"></script> #}
           
            <script src="{{ asset('JS/jquery.js') }}" type="text/javascript"></script>
            <script src="{{ asset('JS/jquery-ui.js') }}" type="text/javascript"></script>
            {# // <script src="{{ asset('JS/mio.js') }}" type="text/javascript"></script>
            // <script src="{{ asset('JS/mesa.js') }}" type="text/javascript"></script> #}
            <script src="{{ asset('JS/mio2.js') }}" type="text/javascript"></script>
            <script src="{{ asset('JS/mesas2.js') }}" type="text/javascript"></script>
            <script src="{{ asset('JS/crearMesas.js') }}" type="text/javascript"></script>
            {# <script src="{{ asset('JS/validaformulario.js') }}" type="text/javascript"></script> #}
            <script src="{{ asset('JS/reserva.js') }}" type="text/javascript"></script>
            <script src="{{ asset('Bootstrap/solartec-1.0.0/lib/wow/wow.min.js') }}" type="text/javascript"></script>
            {% block javascripts %}

            {% endblock %}
        
    </head>
    <body>
    <nav class="navbar navbar-expand-lg bg-white navbar-light shadow sticky-top p-0">
        <a href="{{ path('app_home') }}" class="navbar-brand d-flex align-items-center px-4 px-lg-5">
            <h2 class="m-0 text-primary"><i class="fa fa-book me-3"></i>Proyecto</h2>
        </a>
        <button type="button" class="navbar-toggler me-4" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <div class="navbar-nav ms-auto p-4 p-lg-0">
                <a href="{{ path('app_home') }}" class="nav-item nav-link active">Home</a>
                <a href="{{ path('reserva') }}" class="nav-item nav-link">RESERVAS</a>
                <a href="courses.html" class="nav-item nav-link">EVENTOS</a>
                <div class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Pages</a>
                    <div class="dropdown-menu fade-down m-0">
                        <a href="{{ path('app_mi_mesas') }}" class="dropdown-item">Mesas</a>
                        <a href="{{ path('app_mi_juegos') }}" class="dropdown-item">Juegos</a>
                        <a href="{{ path('reserva') }}" class="dropdown-item">Reserva</a>
                        <a href="404.html" class="dropdown-item">404 Page</a>
                    </div>
                </div>
                {# {{dump(app.user.roles)}} #}
                <a href="{{path('app_contact')}}" class="nav-item nav-link">Contacto</a>
                {% if app.user %}
                    {% if app.user.roles|length > 1 %}
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">MANTENIMIENTO</a>
                            <div class="dropdown-menu fade-down m-0">
                                <a href="{{ path('admin') }}" class="dropdown-item">USUARIOS</a>
                                <a href="{{ path('app_listado_juegos') }}" class="dropdown-item">JUEGOS</a>
                            </div>
                        </div>
                    {% endif %}
                {% endif %}
            </div>
            {% if app.user %}
            {# {{dump(app.user.roles)}} #}
                <div class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle me-3" data-bs-toggle="dropdown">{{app.user.email}}</a>
                    <div class="dropdown-menu fade-down ms-3">
                        <a href="{{ path('app_logout') }}" class="dropdown-item">Cerrar Sesion</a>
                        <a href="/edit" class="dropdown-item">Editar Usuario</a>
                        
                    </div>
                </div>
            {% else %}
            
                <a href="{{ path('app_login') }}" class="btn btn-primary py-4 px-lg-5 d-none d-lg-block">Inicia Sesion<i class="fa fa-arrow-right ms-3"></i></a>
            {% endif %}
            
        </div>
    </nav>
        {% block body %}
            

        {% endblock %}
            <footer>
        <div>
            <div class="social">
                <a href="#"><img src="{{ asset('Imagenes/facebook.png') }}" alt="facebook"></a>
                <a href="#"><img src="{{ asset('Imagenes/youtube.png') }}" alt="youtube"></a>
                <a href="#"><img src="{{ asset('Imagenes/twitter.png') }}" alt="twitter"></a>
                <a href="#"><img src="{{ asset('Imagenes/instagram.jpg') }}" alt="instagram"></a><br>
                <small class="mt-5">Copyright © Departamento de Informática 2019</small>
            </div>
        </div>
    </footer>
    </body>
</html>
